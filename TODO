Make the encrypt/decrypt part of the interactive menu. Maybe also make the menu
look a bit nicer.

Add configurable building so the program can be built with/without ncurses
support.

Cryptanalysis

Config files
